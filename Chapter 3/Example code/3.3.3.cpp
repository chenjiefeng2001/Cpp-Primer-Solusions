/**
 * 其他vector操作
 * v.empty()    如果v不含有任何元素，返回为真，否则返回假（常常与!连用，用于检测石佛铺位空vector对象）
 * v.size()     返回v中的元素的个数
 * v.push_back(t)   向v的尾部添加一个值为t的元素
 * v[n]         返回v中第n个位置上的元素的引用
 * v1=v2        用v2中的元素拷贝v1中的元素
 * v1=[a,b,c,...]   用列表中的元素拷贝v1中的元素
 *  
 */